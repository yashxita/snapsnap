module.exports = {
    theme: {
      extend: {},
    },
    future: {
      // This option ensures Tailwind doesn't use oklch/oklab and falls back to RGB
      useOklab: false,
    },
    plugins: [],
  }